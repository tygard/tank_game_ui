#!/bin/bash
set -e

# Cd to repo root
cd "$(dirname "${BASH_SOURCE[0]}")/.."

./scripts/build

containerId=$(docker create ghcr.io/ryan3r/tank_game_ui:development)
rm -rf engine
docker cp $containerId:/app/engine/ .
